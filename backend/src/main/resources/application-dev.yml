spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/irms_dev
    username: 
    password: 
  
  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true

logging:
  level:
    com.irms: DEBUG
    org.springframework.web: DEBUG
    org.springframework.security: DEBUG
